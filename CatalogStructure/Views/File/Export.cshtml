@model string

<h1>Export Catalog Structure</h1>

@if (!string.IsNullOrEmpty(Model))
{
    <div class="alert alert-success">
        File successfully saved: @Model
    </div>
}

<form method="post">
    <button type="submit" class="btn btn-primary">Export to File</button>
</form>